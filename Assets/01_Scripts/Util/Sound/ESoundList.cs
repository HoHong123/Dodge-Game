public enum SFXList {
    #region ----- Common -----
    Click = 0,
    Notification,
    CashRegister,
    #endregion
}

public enum BGMList {
    Main,
    Lobby,

    Game,
}

public enum Sound {
    SFX,
    BGM,
}
